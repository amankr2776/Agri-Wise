
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user of the AgriWise application, such as a farmer, government authority, or logistics provider. Stores essential profile information and preferences.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user (optional, for communication).",
          "format": "email"
        },
        "phoneNumber": {
          "type": "string",
          "description": "The primary contact phone number for the user."
        },
        "addressCity": {
          "type": "string",
          "description": "The city where the user primarily resides, used for location-based services like Mandi price mapping and local alerts."
        },
        "addressState": {
          "type": "string",
          "description": "The state where the user primarily resides."
        },
        "preferredLanguage": {
          "type": "string",
          "description": "The user's preferred language for the application interface and voice interactions (e.g., 'en', 'hi', 'bho')."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the system (e.g., 'farmer', 'government_authority', 'logistics_provider', 'expert')."
        },
        "savedCropIds": {
          "type": "array",
          "description": "References to Crop entities that the user has marked as 'saved' to receive personalized price change alerts. (Relationship: UserProfile N:N Crop)",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "phoneNumber",
        "addressCity",
        "addressState",
        "preferredLanguage",
        "role"
      ]
    },
    "LogisticsTicket": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "LogisticsTicket",
      "type": "object",
      "description": "Represents a reported issue or dispute related to a logistics shipment.",
      "properties": {
        "id": { "type": "string" },
        "shipmentId": { "type": "string" },
        "farmerId": { "type": "string" },
        "farmerName": { "type": "string" },
        "issueType": { "type": "string", "enum": ["Delay", "Damaged Goods", "Pricing Dispute", "Other"] },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["Open", "In Review", "Resolved"] },
        "isEscalated": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["shipmentId", "farmerId", "issueType", "status"]
    },
    "Conversation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Conversation",
      "type": "object",
      "description": "A structured exchange between a user and the AI Assistant.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "prompt": { "type": "string" },
        "answer": { "type": "string" },
        "suggestedAction": { "type": "string" },
        "imageUrl": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "prompt", "answer", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information."
        }
      },
      {
        "path": "/users/{userId}/conversations/{conversationId}",
        "definition": {
          "entityName": "Conversation",
          "schema": {
            "$ref": "#/backend/entities/Conversation"
          },
          "description": "History of AI assistant interactions."
        }
      },
      {
        "path": "/logisticsTickets/{ticketId}",
        "definition": {
          "entityName": "LogisticsTicket",
          "schema": {
            "$ref": "#/backend/entities/LogisticsTicket"
          },
          "description": "Incident reports for logistics disputes."
        }
      }
    ]
  }
}
